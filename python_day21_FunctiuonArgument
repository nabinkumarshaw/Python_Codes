#Different types of Arguments

#1)Default Argument
def avg(a=3,b=5):
  return (a+b)/2

c=avg(4,6)    #5,ignore the default argument that is passed
print(c)
c=avg()
print(c)

#2)Keyword Argument
def avg(a=3,b=5):
  return (a+b)/2

c=avg(b=12,a=4)    #8,order of the parameter does not have any effect
print(c)

#3) variable length Argument
def avg(*num):
  sum=0
  for i in num:
    sum=sum+i
  print("The average is ",sum/len(num))

avg(2,4,6,8,10)    #6 ,you can pass any niumber of parameters to it ,it returns the result

#4)Required Argument
def sum(a,b):
  sum=a+b
  return sum

print(sum(3,7))

